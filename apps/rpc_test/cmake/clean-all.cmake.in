# Clean all generated files

file(REMOVE_RECURSE "${CMAKE_CURRENT_BINARY_DIR}/lib")
file(REMOVE "${CMAKE_CURRENT_BINARY_DIR}/bin/client")
file(REMOVE "${CMAKE_CURRENT_BINARY_DIR}/bin/server")
file(REMOVE "${CMAKE_CURRENT_BINARY_DIR}/bin/benchmarks")
file(REMOVE "${CMAKE_CURRENT_BINARY_DIR}/*.o")
file(REMOVE "${CMAKE_CURRENT_BINARY_DIR}/a.out")
